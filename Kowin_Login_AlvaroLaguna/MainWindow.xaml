<Window x:Class="Kowin_Login_AlvaroLaguna.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="KoWin - Home" Height="900" Width="1300"
        WindowStartupLocation="CenterScreen"
        Background="Black" Foreground="White">

    <Window.Resources>
        <!-- ======================================================= -->
        <!-- 1. ESTILOS DE EFECTOS (AQUÍ ESTÁ LA SOLUCIÓN)           -->
        <!-- ======================================================= -->

        <!-- Estilo para el LOGO (Texto con sombra roja) -->
        <Style x:Key="LogoStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#FF0000"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Red" BlurRadius="15" ShadowDepth="0"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para el MENÚ LATERAL (Borde con sombra suave) -->
        <Style x:Key="SideMenuStyle" TargetType="Border">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="BorderBrush" Value="#FF0000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Red" BlurRadius="10" ShadowDepth="0" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo de la TARJETA DE JUEGO (Cuadrado y Neón Hover) -->
        <Style x:Key="GameCardBorder" TargetType="Border">
            <Setter Property="Background" Value="#0f0f0f"/>
            <Setter Property="BorderBrush" Value="#800000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Red" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#FF0000"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Red" BlurRadius="25" ShadowDepth="0" Opacity="1.0"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ======================================================= -->
        <!-- 2. ESTILOS DE SCROLLBAR                                 -->
        <!-- ======================================================= -->
        <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle" Background="#FF0000" CornerRadius="4"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Foreground" Value="#FF0000"/>
            <Setter Property="Background" Value="#111111"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" Focusable="false">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="12"/>
                    <Setter Property="Height" Value="Auto"/>
                </Trigger>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="12"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- === BARRA SUPERIOR === -->
        <Grid Grid.Row="0" Background="#111111">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <!-- Logo (Ahora usa el estilo LogoStyle, sin líos de etiquetas) -->
            <TextBlock Text="KoWin" Style="{StaticResource LogoStyle}"/>

            <!-- Menú Central -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="TIENDA" Margin="15,0" FontWeight="Bold" Cursor="Hand" Foreground="White"/>
                <TextBlock Text="COMUNIDAD" Margin="15,0" FontWeight="Bold" Cursor="Hand" Foreground="Gray"/>
                <TextBlock Text="SOPORTE" Margin="15,0" FontWeight="Bold" Cursor="Hand" Foreground="Gray"/>
            </StackPanel>

            <!-- Mi Perfil / Cerrar Sesión -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,20,0">
                <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                    <TextBlock x:Name="TxtUsuario" Text="Usuario" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right"/>
                    <TextBlock Text="cerrar sesión" FontSize="10" Foreground="Gray" Cursor="Hand" 
                               HorizontalAlignment="Right" MouseLeftButtonDown="BtnCerrarSesion_Click"
                               TextDecorations="Underline"/>
                </StackPanel>
                <Border Width="32" Height="32" CornerRadius="16" Background="#333333" BorderBrush="#FF0000" BorderThickness="1"/>
            </StackPanel>
        </Grid>

        <!-- === CONTENIDO PRINCIPAL === -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- MENÚ LATERAL (Ahora usa SideMenuStyle, imposible que falle) -->
            <Border Grid.Column="0" Style="{StaticResource SideMenuStyle}" Margin="0,0,20,0">
                <StackPanel>
                    <TextBlock Text="Destacados" FontSize="18" Margin="0,20,0,15" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
                    <TextBlock Text="Mis juegos" FontSize="18" Margin="0,15" Foreground="#FF0000" FontWeight="Bold" Cursor="Hand"/>
                    <TextBlock Text="Configuración" FontSize="18" Margin="0,15" Foreground="Gray" Cursor="Hand"/>
                    <Separator Margin="0,20" Background="#333333"/>
                    <TextBlock Text="Amigos" FontSize="14" Foreground="Gray" Margin="0,10"/>
                </StackPanel>
            </Border>

            <!-- ZONA DE JUEGOS -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0,0,0,50">

                    <!-- 1. DESTACADOS -->
                    <TextBlock Text="Destacados" FontSize="26" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,30">
                        <ItemsControl x:Name="ListaDestacados">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="10,10,20,10">
                                        <Border Width="400" Height="220" Style="{StaticResource GameCardBorder}">
                                            <Image Source="{Binding ImagenUrl}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <TextBlock Text="{Binding Titulo}" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- 2. LANZAMIENTOS -->
                    <TextBlock Text="Últimos Lanzamientos" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,30">
                        <ItemsControl x:Name="ListaLanzamientos">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="10,10,15,10">
                                        <Border Width="220" Height="300" Style="{StaticResource GameCardBorder}">
                                            <Image Source="{Binding ImagenUrl}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <TextBlock Text="{Binding Titulo}" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" HorizontalAlignment="Center" TextWrapping="Wrap" MaxWidth="220"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- 3. TERROR -->
                    <TextBlock Text="Terror y Suspense" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,30">
                        <ItemsControl x:Name="ListaTerror">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="10,10,15,10">
                                        <Border Width="200" Height="280" Style="{StaticResource GameCardBorder}">
                                            <Image Source="{Binding ImagenUrl}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <TextBlock Text="{Binding Titulo}" Foreground="White" FontSize="14" FontWeight="Bold" TextTrimming="CharacterEllipsis" MaxWidth="200" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- 4. SUPERVIVENCIA -->
                    <TextBlock Text="Supervivencia" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,30">
                        <ItemsControl x:Name="ListaSupervivencia">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="10,10,15,10">
                                        <Border Width="300" Height="150" Style="{StaticResource GameCardBorder}">
                                            <Image Source="{Binding ImagenUrl}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <TextBlock Text="{Binding Titulo}" Foreground="White" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- 5. RPG -->
                    <TextBlock Text="RPG y Aventura" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,20">
                        <ItemsControl x:Name="ListaRPG">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="10,10,15,10">
                                        <Border Width="250" Height="250" Style="{StaticResource GameCardBorder}">
                                            <Image Source="{Binding ImagenUrl}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <TextBlock Text="{Binding Titulo}" Foreground="White" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>